---
// Mockup mobile stylisé — simule l'UI de chacune des 5 apps customApps.
// Variants : 'sales' (SS), 'supply' (SP), 'shipping' (SH), 'locations' (CL), 'fileSync' (FS).
interface Props {
  variant: 'sales' | 'supply' | 'shipping' | 'locations' | 'fileSync';
  class?: string;
}

const { variant, class: className = '' } = Astro.props;

const THEMES = {
  sales:     { from: 'from-blue-500',    to: 'to-blue-600',    accent: 'text-blue-600',    soft: 'bg-blue-50',    border: 'border-blue-100' },
  supply:    { from: 'from-emerald-500', to: 'to-emerald-600', accent: 'text-emerald-600', soft: 'bg-emerald-50', border: 'border-emerald-100' },
  shipping:  { from: 'from-orange-500',  to: 'to-orange-600',  accent: 'text-orange-600',  soft: 'bg-orange-50',  border: 'border-orange-100' },
  locations: { from: 'from-teal-500',    to: 'to-teal-600',    accent: 'text-teal-600',    soft: 'bg-teal-50',    border: 'border-teal-100' },
  fileSync:  { from: 'from-violet-500',  to: 'to-violet-600',  accent: 'text-violet-600',  soft: 'bg-violet-50',  border: 'border-violet-100' },
};

const t = THEMES[variant];
---

<div class={`phone-frame phone-frame-md animate-float ${className}`}>
  <div class="phone-screen">
    {/* Captures d'écran réelles — remplissent tout l'écran (status bar Android incluse) */}
    {variant === 'sales' && (
      <img
        src="/im/customSales.webp"
        alt="Capture d'écran customSales — fiche client avec encours, devis et commandes"
        class="absolute inset-0 w-full h-full object-cover object-top"
        loading="lazy"
        decoding="async"
      />
    )}
    {variant === 'supply' && (
      <img
        src="/im/customSupplies.webp"
        alt="Capture d'écran customSupplies — panier de commande fournisseur avec plafond franco"
        class="absolute inset-0 w-full h-full object-cover object-top"
        loading="lazy"
        decoding="async"
      />
    )}
    {variant === 'locations' && (
      <img
        src="/im/customLocations.jpeg"
        alt="Capture d'écran customLocations — saisie d'emplacement d'entrepôt par scan (rack, colonne, étage, position)"
        class="absolute inset-0 w-full h-full object-cover object-top"
        loading="lazy"
        decoding="async"
      />
    )}

    {/* Status bar factice (uniquement pour les variantes sans capture réelle) */}
    {variant !== 'sales' && variant !== 'supply' && variant !== 'locations' && (
    <div class="flex items-center justify-between px-6 pt-8 pb-2 text-xs font-semibold text-gray-900 dark:text-gray-100">
      <span>9:41</span>
      <div class="flex items-center gap-1">
        <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24"><path d="M1 21h22L12 2 1 21z"/></svg>
        <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24"><path d="M17 7h-2v2h2V7zm0 4h-2v2h2v-2zm-4-4H7v2h6V7zm0 4H7v2h6v-2zM5 15h14v2H5v-2zm14-10H5v2h14V5z"/></svg>
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M15.67 4H14V2h-4v2H8.33C7.6 4 7 4.6 7 5.33v15.33C7 21.4 7.6 22 8.33 22h7.33c.74 0 1.34-.6 1.34-1.33V5.33C17 4.6 16.4 4 15.67 4z"/></svg>
      </div>
    </div>
    )}

    {variant === 'shipping' && (
      <div class="px-4 pt-2">
        <div class="mb-3">
          <div class="text-[10px] text-gray-400 dark:text-gray-400 uppercase tracking-wider">Tournée #42</div>
          <div class="font-bold text-gray-900 dark:text-gray-100 text-lg">Livraisons du jour</div>
        </div>

        <div class={`relative rounded-xl overflow-hidden h-32 bg-gradient-to-br ${t.from} ${t.to} mb-3`}>
          <svg class="absolute inset-0 w-full h-full opacity-30" viewBox="0 0 200 100" preserveAspectRatio="none">
            <path d="M0,70 Q50,30 100,50 T200,40" stroke="white" stroke-width="1.5" fill="none" stroke-dasharray="3,3"/>
            <circle cx="20"  cy="68" r="4" fill="white"/>
            <circle cx="60"  cy="40" r="4" fill="white"/>
            <circle cx="110" cy="50" r="4" fill="white"/>
            <circle cx="170" cy="43" r="4" fill="white"/>
          </svg>
          <div class="absolute inset-0 p-3 flex flex-col justify-between text-white">
            <div class="flex items-center justify-between">
              <span class="text-[10px] font-semibold bg-white/20 backdrop-blur rounded-full px-2 py-0.5">EN ROUTE</span>
              <span class="text-[10px] opacity-80">4 arrêts</span>
            </div>
            <div>
              <div class="text-[10px] opacity-80">Prochain arrêt</div>
              <div class="font-bold text-sm">12 rue des Artisans, Lyon</div>
            </div>
          </div>
        </div>

        <div class="space-y-1.5">
          {[
            { n: 1, addr: 'Quincaillerie Martin', status: 'Livré',    color: 'text-green-600', done: true },
            { n: 2, addr: 'Atelier Moreau',       status: 'Livré',    color: 'text-green-600', done: true },
            { n: 3, addr: 'Garage Petit',         status: 'En cours', color: t.accent,         done: false },
            { n: 4, addr: 'Menuiserie Blanc',     status: 'Prévu',    color: 'text-gray-400 dark:text-gray-400',  done: false },
          ].map((s) => (
            <div class="flex items-center gap-2 py-1.5">
              <div class={s.done
                ? 'w-6 h-6 rounded-full bg-green-500 text-white flex items-center justify-center text-[10px] font-bold'
                : (s.status === 'En cours'
                  ? `w-6 h-6 rounded-full bg-gradient-to-br ${t.from} ${t.to} text-white flex items-center justify-center text-[10px] font-bold animate-pulse`
                  : 'w-6 h-6 rounded-full bg-gray-100 dark:bg-gray-800 text-gray-500 dark:text-gray-300 flex items-center justify-center text-[10px] font-bold')}>
                {s.done ? '✓' : s.n}
              </div>
              <div class="flex-1 min-w-0">
                <div class="text-xs font-semibold text-gray-900 dark:text-gray-100 truncate">{s.addr}</div>
              </div>
              <span class={`text-[10px] font-semibold ${s.color}`}>{s.status}</span>
            </div>
          ))}
        </div>
      </div>
    )}

    {variant === 'fileSync' && (
      <div class="px-4 pt-2">
        <div class="mb-3">
          <div class="text-[10px] text-gray-400 dark:text-gray-400 uppercase tracking-wider">Synchronisation</div>
          <div class="font-bold text-gray-900 dark:text-gray-100 text-lg">Documents métier</div>
        </div>

        <div class={`${t.soft} border ${t.border} rounded-xl p-3 mb-3`}>
          <div class="flex items-center gap-2 mb-2">
            <div class={`w-2 h-2 rounded-full bg-green-500 animate-pulse`}></div>
            <span class="text-[10px] font-semibold text-gray-900 dark:text-gray-100">Sync active · 2 min</span>
          </div>
          <div class="flex items-baseline gap-1">
            <span class={`text-2xl font-extrabold ${t.accent}`}>342</span>
            <span class="text-[10px] text-gray-500 dark:text-gray-300">fichiers sur 12 Go</span>
          </div>
          <div class="w-full bg-white/60 rounded-full h-1 mt-2 overflow-hidden">
            <div class={`h-full bg-gradient-to-r ${t.from} ${t.to} rounded-full`} style="width: 68%"></div>
          </div>
        </div>

        <div class="text-[10px] font-semibold text-gray-400 dark:text-gray-400 uppercase tracking-wider mb-2">Récemment mis à jour</div>
        <div class="space-y-1.5">
          {[
            { name: 'Catalogue 2026.pdf',     size: '4.2 Mo', time: "à l'instant", locked: true },
            { name: 'Fiche-tech-ref-A42.pdf', size: '1.1 Mo', time: '2 min',       locked: false },
            { name: 'Certification-ISO.pdf',  size: '230 Ko', time: '1 h',         locked: true },
            { name: 'CGV-2026.docx',          size: '78 Ko',  time: '3 h',         locked: false },
          ].map((f) => (
            <div class="bg-white dark:bg-gray-900 border border-gray-100 dark:border-gray-700 rounded-xl p-2 flex items-center gap-2.5">
              <div class={`w-8 h-8 rounded-lg bg-gradient-to-br ${t.from} ${t.to} flex items-center justify-center flex-shrink-0`}>
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
              </div>
              <div class="flex-1 min-w-0">
                <div class="flex items-center gap-1">
                  <span class="text-xs font-semibold text-gray-900 dark:text-gray-100 truncate">{f.name}</span>
                  {f.locked && (
                    <svg class="w-2.5 h-2.5 text-gray-400 dark:text-gray-400 flex-shrink-0" fill="currentColor" viewBox="0 0 24 24"><path d="M12 1a5 5 0 00-5 5v4H5a2 2 0 00-2 2v10a2 2 0 002 2h14a2 2 0 002-2V12a2 2 0 00-2-2h-2V6a5 5 0 00-5-5zm0 2a3 3 0 013 3v4H9V6a3 3 0 013-3z"/></svg>
                  )}
                </div>
                <div class="text-[10px] text-gray-500 dark:text-gray-300">{f.size} · {f.time}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    )}
  </div>
</div>
